from setuptools import setup, find_packages

setup(
   name='rd_api_py',
   version='0.1',
   description='Python library for Real Debrid API',
   author='$krilla',
   packages=find_packages(),
   install_requires=[],
)